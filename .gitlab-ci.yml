stages:
  - build
  - test
  - deploy

build_job:
  stage: build
  script: python main.py bdist --format = zip


test_job:
  stage: test
  script: pytest -v

deploy_job:
  stage: deploy
  script: echo 'Start'
  when: manual
